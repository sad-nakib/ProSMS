rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public access for single-user mode (Login removed)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
